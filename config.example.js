// Kopiera denna fil till "config.js" och fyll i dina egna värden
// från Supabase-projektets Settings → API.
// Project URL och anon public key är INTE hemliga — de är gjorda för
// att ligga i frontend-kod. Säkerheten sköts av RLS-policyerna i sql/schema.sql.

window.SUPABASE_CONFIG = {
  url: 'https://ditt-projekt.supabase.co',
  anonKey: 'din-anon-public-key'
};
